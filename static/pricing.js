(()=>{const target=document.getElementById('catalog');const money=(n,c)=>(Number(n)/100).toLocaleString('en-IN',{style:'currency',currency:c});fetch('/api/catalog',{cache:'no-store'}).then(async r=>{if(!r.ok)throw Error('Catalog unavailable');return r.json()}).then(data=>{target.innerHTML=data.items.map(p=>`<article class="card"><small>${p.product_type.replaceAll('_',' ')}</small><h2>${escapeHtml(p.name)}</h2><p>${escapeHtml(p.description||'Structured MeritIQra access')}</p>${p.list_amount_minor!==p.sale_amount_minor?`<del>${money(p.list_amount_minor,p.currency)}</del> `:''}<strong>${money(p.sale_amount_minor,p.currency)}</strong><p>${p.term_days?`${p.term_days} days`:''}${p.credit_quantity?` · ${p.credit_quantity} test credits`:''}</p><a href="/app">Sign in to continue</a></article>`).join('')||'<p>No paid offerings are active yet. Please enquire for details.</p>'}).catch(e=>target.textContent=e.message);function escapeHtml(v){const d=document.createElement('div');d.textContent=v;return d.innerHTML}})();
